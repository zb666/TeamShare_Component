组件化
就是在模块化的基础之上进行动态的切换，当一个模块切换成app的时候，会涉及到gradle,清单文件，代码的一系列配置

注意事项
需要单独切成moudle的时候  打开config.gradle的文件
然后 修改isMoudle变量，然后同步项目，但是你会发现这时候会报错，因为R文件资源的指向已经发生变化了，
从R.common 变成了 R.moudleb ，这个时候只要删除错误导包，然后快捷键导入r文件就行了.